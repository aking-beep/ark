## Round 1 — 2026-09-16 — 5/5

From the artifacts alone, before any diff: a configured Ollama `complete()` used to `POST /api/chat` with whatever id the caller named (`before.json`: 0 `/api/tags` GETs; missing `deepseek-r1` still posted). After, it `GET`s `/api/tags` first, POSTs an installed match (exact, `:latest`, or unique family), and refuses a miss with `errorKind: config` and no chat POST. Live daemon on this host still has only `smollm2:135m`; `deepseek-r1` / default `llama3.2` fail without pulling.

| # | Criterion | Point | Finding |
|---|---|---|---|
| 1 | Spec satisfied | ✅ | All four acceptance criteria met as written. Inventory-before-chat, installed-id resolution (`smollm2:135m`, unique `smollm2` → `smollm2:135m`, `llama3.2` → `llama3.2:latest`, Hub `huggingface.co/…` → `hf.co/…`), missing/empty/ambiguous refuse with `kind: 'config'` and zero chat POSTs, opt-in `allowPull` / `ARK_OLLAMA_PULL=1` skips tags, empty env stays unconfigured. Tests, `smoke:runtime`, and live inventory-unchanged are in the evidence. No undeclared scope (`listModels()` absent; no extra adapter; no silent family substitution). |
| 2 | Evidence proves it | ✅ | Same probe (`evidence/runtime-local-inventory/probe.mjs`) before and after on mocked fetch. `captures.tsv` stamps before at `0841118` dirty=0 (product still had no `/api/tags`) and after at `dd5bad3`. `before.json` vs `after.json` show the behavior change without reading the diff. Live supporting pair: daemon 404 on `POST /api/chat` for `deepseek-r1` (`before-live-missing.json`) vs Runtime config-fail and unchanged `["smollm2:135m"]` (`after-live.json`). “What this does not prove” names auto-pull daemons, live `ARK_OLLAMA_PULL=1`, and Hub-id family mapping. Tests are support, not the artifact. |
| 3 | Structure holds | ✅ | Change lives in the Ollama adapter (`packages/providers/src/ollama.ts`) and env construction (`from-env.ts`). `GET /api/tags` uses existing `fetchWithTimeout`; no edge talks to a provider; no service imports a framework request type; no new dependency. |
| 4 | Fails safely | ✅ | New tags call has the same timeout as chat (`ollama.ts:133–138`). HTTP/network/parse failures throw via `readJson` before any chat POST. A miss throws `ProviderError('config')` with requested id and installed list (`ollama.ts:145–149`). Empty/malformed tags body parses to `[]` and refuses rather than pulling. `ARK_OLLAMA_PULL` is env-gated and documented. Runtime already maps a config miss onto fallback / ingest (`execute.test.ts:176–204`). |
| 5 | Readable | ✅ | `resolveOllamaTag` / `parseOllamaTags` / `allowPull` names match the spec. Comments state why (no cross-family substitute; skip tags only when pull is opted in). Logic sits in one adapter file; tests cover exact, latest, unique family, Hub rewrite, missing, empty, ambiguous, and env pull. No dead code. |

**Blocking:** none.
**Non-blocking:** `after-live.json:32` reports `errorKind: "unknown"` because `live.mjs:54` reads `kind` on the wrapper thrown at `packages/runtime/src/execute.ts:68`; the adapter kind is `config` in `after.json:52`. `adapters.test.ts:41` does not assert a tags GET count on the happy path (refuse tests and `after.json` do). There is no dedicated test that a down `/api/tags` stays fail-closed; that follows from `resolveInstalled` awaiting `fetchWithTimeout`/`readJson` before chat.
