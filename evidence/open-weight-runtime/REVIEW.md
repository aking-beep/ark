## Round 1 — 2026-09-16 — 5/5

| # | Criterion | Point | Finding |
|---|---|---|---|
| 1 | Spec satisfied | ✅ | All four acceptance criteria met. After-probe JSON shows `hintAdapter` → `ollama` for the named open-weight ids and not for `gpt-4o` / `gpt-5-nano` / `amazon.nova-lite-v1:0` / `claude-sonnet-5`; `deepseek-chat` / `deepseek-reasoner` → `openai-compatible`; `DEEPSEEK_API_KEY` POSTs `https://api.deepseek.com/v1/chat/completions` with default `deepseek-chat`; empty env leaves both adapters unconfigured. `execute.test.ts` covers Ollama receiving `deepseek-r1` (not `llama3.2`) and stripping `gpt-4o` on fallback. `after-test.txt`, `after-smoke.txt`, and `after-live-ollama.txt` cover AC4. |
| 2 | Evidence proves it | ✅ | Same `probe.mjs` before (`d3d659f`, product still v0.2) and after (`74ab52d`): open-weight hints flip from `null` to `ollama`; DeepSeek env flips from unconfigured/`error` to `api.deepseek.com`. `captures.tsv` stamps both. `EVIDENCE.md` “what this does not prove” is specific (no live DeepSeek; only `smollm2:135m` pulled). Tests support; they are not the artefact. |
| 3 | Structure holds | ✅ | Catalog/hint logic stays in `packages/runtime/src/catalog.ts` (typed ids, no framework `Request`). DeepSeek host is constructed only in `packages/providers/src/from-env.ts` on the existing OpenAI-compatible adapter. `parse.ts` stays in the adapter package. No new dependency; no fourth adapter; no edge talking to a provider client. |
| 4 | Fails safely | ✅ | Hosted DeepSeek reuses `OpenAICompatibleAdapter`’s 30s timeout (`openai-compatible.ts:48`). Empty env still reports both adapters unconfigured, so there is no surprise outbound call. Unconfigured Ollama still fails as `ProviderError('config')`. Runtime fallback and `maxCostUsd` constraints are unchanged. Key is read from env and documented in `.env.example`. |
| 5 | Readable | ✅ | Diff is the three files the spec named plus tests. `hostedDeepSeek`, the exact-id DeepSeek branch, and the 32B→14B comment explain the non-obvious routing/pricing choices. Tests sit next to the logic they cover. |

**Blocking:** none.
**Non-blocking:** `packages/providers/src/from-env.ts:32` still uses `DEEPSEEK_API_KEY` as the frontier Bearer whenever `ARK_FRONTIER_API_KEY` is empty, even if `ARK_FRONTIER_BASE_URL` points at another host; scoping the DeepSeek key to the `hostedDeepSeek` branch would keep that secret on `api.deepseek.com`. `packages/runtime/src/catalog.ts:50` matches families with `includes` on short tags (`yi`, `phi`, `aya`); the spec’s closed-source examples are already excluded by earlier branches, but a word-boundary would be stricter. Live evidence is `smollm2:135m`, not DeepSeek-R1 — already stated in `EVIDENCE.md`.
