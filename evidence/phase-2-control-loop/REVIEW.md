## Round 1 — 2026-09-15 — 5/5

From the artefacts alone, before any code: an unauthenticated operator who could open Control and ingest events can no longer do either. `GET /dashboard` is 307 to `/login` (was 200 with Spend / Support ticket). Bearer-less ingest is 401 (was 202). A Demo Co token posting `orgId` Northwind is 403 `org_mismatch`. A Northwind session sees Freight claims and not Support ticket; a Demo Co session is the reverse. Northwind starts at 0 events, calibration is `calibrated` (n=961 fleet) then `measured` (n=40) after `ingest:live`. A `block` budget refuses the follow-up event (`accepted: 0`, `budget_block`). Alerts POST to a webhook and land in `alert_deliveries`.

| # | Criterion | Point | Finding |
|---|---|---|---|
| 1 | Spec satisfied | ✅ | All five acceptance criteria are met as written. HTML routes take org from `requireOrg()`; unauthenticated `/dashboard` redirects; ingest and calibration take org from bearer/session, not `?org=`; token A posting org B is 403 and the write uses the token's org. `deliverAlerts` POSTs webhook JSON or Slack `{text}`, times out at 3s, records 5xx as `ok=0`, and does not fail the 202. `block` skips the insert and returns `budget_block`; `throttle` still inserts and returns `budget_throttle`; `observe`/`warn` stay allow. Seed creates `org_northwind` with zero events; `ingest:live` calls `applyIngest`; calibration flips `calibrated` → `measured` at ≥30 traces. Consumer/business still have no `@ark/db`. Compose maps 3000/3001/3002 with a durable volume. `BasisTag` copy for `calibrated` is fleet-not-yours. Deviations (`?org=` ignored; open ingest only pre-seed) are declared in `EVIDENCE.md`. |
| 2 | Evidence proves it | ✅ | `before.txt` at 86e0440 and `after.txt` at c691332, same port 3002, same `file:/workspace/ark.db`, HTTP + SQL + HTML dumps. Side-by-side table matches the artefacts. “What this does not prove” names live-generated traces, Docker daemon absence, local webhook sink, HMAC-not-encrypted cookies, and no public host. Not test-output-only. |
| 3 | Structure holds | ✅ | New persistence and outbound HTTP live in `packages/db` (`auth.ts`, `alerts.ts`, `ingest.ts`). Pure budget policy is in `packages/core/src/ops/budget-enforcement.ts` with no framework types. Routes parse and call those functions; `fetch` for destinations is constructed only in `deliverAlerts`. No new runtime dependency. Control pages already queried `@ark/db`; this diff swaps a hardcoded org id for the session org rather than introducing a new edge→SQL path. |
| 4 | Fails safely | ✅ | Webhook `fetch` uses `AbortController` + `timeoutMs` (default 3s). Destination failures are written to `alert_deliveries` and do not fail ingest 202. Login failures redirect to `?error=1` or JSON 401. Ingest rejects invalid JSON (400), schema (422), missing bearer (401), org mismatch (403). Session verify is HMAC + expiry. `ARK_SESSION_SECRET` is required in production. Passwords and ingest tokens are hashed at rest. |
| 5 | Readable | ✅ | Names match the spec (`orgFromBearer`, `budget_block`, `ingest:live`, `org_mismatch`). Comments that remain explain why (fleet calibration omits source org; first event past a block ceiling is observed). Tests sit next to auth, session HMAC, alert delivery, budget policy, and ingest block. History is spec → before evidence → feat → after evidence. |

**Blocking:** none.

**Non-blocking:**
- `evidence/phase-2-control-loop/captures.tsv:1` stamps the before capture `dirty=1`. The before body is still the open dashboard / 404 login / 202 ingest of commit 86e0440, so this is not a reconstructed after-the-fact baseline; committing the evidence files before the stamp would make the flag green.
- `packages/db/src/ingest.test.ts` covers `budget_block` refusing a follow-up row; `throttle` still-inserts is only asserted in `packages/core/src/ops/budget-enforcement.test.ts:11`. An `applyIngest` case that stores the event *and* returns `budget_throttle` would match acceptance criterion 3 the same way block does.
- `apps/control/src/app/api/v1/events/route.ts:45` swallows any throw from `deliverAlerts` after the batch is persisted. Per-destination misses are already recorded inside `deliverAlerts`; logging the outer failure (still returning 202) would make a destination-table outage visible.
- `packages/core/src/calibration/client.ts:26` still sets `?org=` when `opts.orgId` is passed. The server ignores it (declared deviation); dropping the query param would match the new contract.
- `apps/business/next-env.d.ts` is unrelated generated noise in the feat diff.
- `after.txt` records `compose_config_skipped (no docker or invalid)`. Spec out-of-scope allows shipping the file without a public host; a `docker compose config` (or up) on a daemon would close the remaining gap on criterion 5 of the spec.
- `packages/db/src/alerts.test.ts` proves webhook JSON and timeout; it does not post a Slack `{text}` body or a 5xx status. Those paths exist in `packages/db/src/alerts.ts:49` and `:66`.
