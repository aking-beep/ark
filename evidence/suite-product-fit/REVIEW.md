## Round 1 — 2026-09-16 — 5/5

| # | Criterion | Point | Finding |
|---|---|---|---|
| 1 | Spec satisfied | ✅ | All six acceptance criteria met. Today defaults are blank (`apps/business/src/components/intake.tsx:118-120`); empty values are omitted from the encoded workload (`:161-163`); payback stays **unknowable** when both are missing (`apps/business/src/app/report/page.tsx:140`); human error rate remains blank; “leave blank” copy is still true (`intake.tsx:455`). `apps/business` depends on `@ark/runtime`; `POST /api/measure` parses a Workload, refuses `not-ai` with 422 without a provider, otherwise runs one synthetic `execute` (id + tasks, never `description`); `GET /api/measure` documents the contract; no `apps/runtime`; consumer does not import `@ark/core` or `@ark/db`. Empty Control copy and login name `POST /api/measure`, `ARK_CONTROL_URL`, and `ARK_CONTROL_TOKEN` and do not print a bearer (`apps/control/src/app/dashboard/page.tsx:179-188`, `login/page.tsx:55-56`). Uncalibrated banner names the same env vars and measure path (`report/page.tsx:473-478`); non-`not-ai` reports offer `MeasureSample` (`:484`); failures are JSON + UI message; timeout 15s. Registry adds DeepSeek, Grok, Microsoft Copilot, Meta AI, GitHub Copilot, LM Studio, and Mistral Le Chat, plus Qwen / DeepSeek-R1 / Llama / Gemma / gpt-oss family rows, all `registry_version` `0.4` with dated official evidence. Result lists use index-qualified keys (`apps/consumer/src/components/results-view.tsx:385-386`, `:523-524`, `:649`). Gemma 3 vs Gemma 4 and `grok.com` vs `x.ai/grok` are declared in `EVIDENCE.md`. |
| 2 | Evidence proves it | ✅ | Same command twice: `node evidence/suite-product-fit/probe.mjs`. Before (`captures.tsv`: `883409c`, dirty=0, spec commit, before the feat) vs after (`b3c536d`). A reader who has not opened the diff can see: GET `/api/measure` 404 → 200 documenting POST; Today defaults `8`/`65` → `''`/`''`; `key={item}` 3 → 0; products 16 → 23 and models 4 → 9; Control empty copy drops `npm run setup` and names `/api/measure` + `ARK_CONTROL_TOKEN` with no bearer secret; live `not-ai` POST is 422 (`after-measure-not-ai.json`); live support POST is 503 `no_provider` (`after-measure-support.json`). Control 1440×900 shots and HTML dumps show the empty-org and login copy. “What this does not prove” is specific: live Ollama was not configured, ingest `ok` is unit tests not this daemon, one sample does not clear the 30-trace floor. |
| 3 | Structure holds | ✅ | Edge `apps/business/src/app/api/measure/route.ts` parses JSON, calls one service (`measureWorkload`), returns JSON — no provider client, no database. Service `apps/business/src/lib/measure.ts` takes a `Workload` and returns a typed result; it does not import `Request`/`NextRequest`. Provider construction stays inside `@ark/runtime` via `execute` / `createRuntime` (authorized by the spec; no new third-party npm dependency; `@ark/runtime` already in the workspace). UI `MeasureSample` posts to the same-origin route. Tests live next to the service (`measure.test.ts`). |
| 4 | Fails safely | ✅ | Invalid JSON is 400 (`route.ts:23-24`); bad Workload is 422 (`:28-32`); `not-ai` is 422 and does not call a provider (`measure.ts:49-55`, test `:61-73`); no adapter is 503 `no_provider` (`route.ts:9`, `measure.ts:66-67`); hung adapter loses to a 15s `Promise.race` (`measure.ts:74-91`, test `:113-136`); other execution errors are 502 with a sliced message (`:69-70`). Cost ceiling `maxCostUsd` 0.05 and `maxTokens` 64 (`:30-32`). UI shows `body.message` or a status string, never a stack (`measure-sample.tsx:31-47`, `:78`). Tokens are named as env vars, not echoed (`dashboard/page.tsx:186-188`; probe `emptyContainsBearerSecret: false`). Policy refusal is not ingested (existing Runtime rule, declared). |
| 5 | Readable | ✅ | Names (`measureWorkload`, `sampleRequestFor`, `MeasureSample`) match the job. Comments explain why the prompt omits `description` (`measure.ts:20-22`) and why measure is not a gateway (`route.ts:14-17`). Tests cover the logic that matters (no description, not-ai, ingest, no_provider, timeout). Change is in the files a reader would open for each AC. No dead code or debug output. |

**Blocking:** none.

**Non-blocking:**
- `evidence/suite-product-fit/after-teams-assess.png` is intake step 1 (“The job”), not the Today fields. Blank defaults are in `after.json` (`todayMinutesDefault` / `todayHourlyDefault`) and `intake.tsx:118-119`, not in that screenshot.
- Live `POST /api/measure` for support triage returned 503 `no_provider` (`after-measure-support.json`); ingest `ok` is the fake-adapter test in `apps/business/src/lib/measure.test.ts:76-105`, already stated in `EVIDENCE.md`.
- `apps/business/src/components/measure-sample.tsx:18-22` — browser `fetch` has no `AbortSignal`. The 15s ceiling is the server race in `measure.ts:74-91`; a hung Next process would still spin “Measuring…” until the connection dies.
- `measure.ts:89-90` — timeout does not abort the in-flight `execute`. A late completion can still ingest after the client has been told `timeout`. Cost is bounded (`maxCostUsd` 0.05); one sample still does not clear calibration.
- Captured Control HTML includes Next.js RSC flight payload with an `ark_session` JWT (`after-control-empty.html`). That is the signed-in dump, not `ARK_CONTROL_TOKEN`; the visible copy does not print a bearer.
