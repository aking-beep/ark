## Round 1 — 2026-09-16 — 5/5

From the artifacts alone (before reading the diff): a caller with only `OPENAI_API_KEY` can now complete through Runtime at `https://api.openai.com/v1/chat/completions` with the model on the request; `chatgpt-4o-latest` and `ft:gpt-4o-mini:…` hint to `openai-compatible`; Whisper / DALL·E / embeddings / TTS stay unselected; GPT-5 sends `max_completion_tokens` not `max_tokens`; o3-mini omits `temperature`; array `content` parts parse as `"Hello"` instead of `""`.

| # | Criterion | Point | Finding |
|---|---|---|---|
| 1 | Spec satisfied | ✅ | All four acceptance criteria are met. AC1: `OPENAI_API_KEY` alone yields `configured() === true`, empty `defaultModel()`, and a POST to `https://api.openai.com/v1/chat/completions`; `ARK_FRONTIER_*` still takes precedence; missing key and URL stays unconfigured. AC2: `hintAdapter` selects `openai-compatible` for `gpt-*`, `chatgpt-*`, `o{digit}*`, and `ft:<those>`, and does not for `text-embedding-3-small`, `whisper-1`, `dall-e-3`, `tts-1`. AC3: GPT-5 / o-series send `max_completion_tokens` and not `max_tokens`; o-series omit `temperature`; other chat models still send `max_tokens`; array `{ text }` parts join. AC4: unit tests cover 1–3; `npm test` exit 0; `npm run smoke:runtime` ok with live skipped. No undeclared scope (no SDK, no catalog price rows, no `apps/runtime`). |
| 2 | Evidence proves it | ✅ | Same probe (`evidence/openai-runtime-path/probe.mjs`) against compiled packages with mocked `fetch`. `before-probe.txt` at `d3d659f` vs `after-probe.txt` at `a2f32f6`; `captures.tsv` timestamps 05:37:28Z / 05:40:34Z. Dirty=2 on before is the spec and the probe; product code was still the merge base. `EVIDENCE.md` “what this does not prove” is specific (no live `api.openai.com`, mocked HTTP, unpriced SKUs). Tests/smoke are supporting, not the comparison. |
| 3 | Structure holds | ✅ | Work stays in the adapter (`packages/providers/src/from-env.ts`, `openai-compatible.ts`, `parse.ts`) and the existing runtime catalog hint (`packages/runtime/src/catalog.ts:47` imports `isOpenAIChatModel` downward from `@ark/providers`). No edge file talks to a provider client; no service imports a framework request type; `fetch` is still constructed only inside the adapter. No new dependency; EVIDENCE.md declares none. |
| 4 | Fails safely | ✅ | The Chat Completions call still goes through `fetchWithTimeout` (`packages/providers/src/openai-compatible.ts:81–94`, default 30s). Missing URL/key and missing model throw `ProviderError` `config` (`openai-compatible.ts:63–76`), not a hang or empty parse. `contentText` (`parse.ts:57–68`) does not throw on unknown part shapes. Default `api.openai.com` is only applied when a key is present (`from-env.ts:32–37`). Keys stay in env; probe/smoke do not print them. Fallback and ingest behaviour are unchanged. |
| 5 | Readable | ✅ | Names match the behaviour (`isOpenAIChatModel`, `usesMaxCompletionTokens`, `omitsTemperature`, `openaiChatBody`). Comments state why (per-request model; avoid a wasted 400). Tests live next to the code they pin. Diff matches the spec’s file list. |

**Blocking:** none.
**Non-blocking:**
- `packages/providers/src/from-env.test.ts:6` is titled “official Chat Completions host” but only asserts `configured()` and `defaultModel()`. The POST URL from `adaptersFromEnv({ OPENAI_API_KEY })` is proven by the probe and by `adapters.test.ts` with a hardcoded `baseUrl`. Asserting that URL in the from-env test would pin AC1 in one place.
- `packages/providers/src/from-env.test.ts:14–26` checks that `ARK_FRONTIER_*` wins on `defaultModel()` only, not on base URL or key.
- `packages/runtime/src/smoke.ts:88–94` live `execute` still omits `model` and uses `maxTokens: 16`. The spec’s prove section asked for `maxTokens: 8` on a cheap chat model. With only `OPENAI_API_KEY` (empty default model), live smoke will now attempt and surface a config error instead of completing.
- `packages/providers/src/normalize.test.ts:91–110` does not exercise a GPT-4.1 or `chatgpt-*` body; those ids share the GPT-5 `max_completion_tokens` branch in `parse.ts:28–36`.
