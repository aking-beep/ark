## Round 1 — 2026-09-16 — 5/5

| # | Criterion | Point | Finding |
|---|---|---|---|
| 1 | Spec satisfied | ✅ | All four acceptance criteria met: `execute` runs policy → router → provider → eval → Control ingest on one `RuntimeRequest` across the three adapters (`packages/runtime/src/execute.ts:30`); no `apps/runtime`; `privacy: 'local-only'` drops cloud adapters before ranking (`packages/runtime/src/policy.ts:66`) and the router explains the choice (`packages/runtime/src/router.ts:62`); fallback is the policy-eligible chain capped by `maxFallbacks` (`packages/runtime/src/router.ts:71`); ingest uses `@ark/sdk` with `sample` omitted and failures returned (`packages/runtime/src/telemetry.ts:82`); provenance uses `heuristic \| benchmark \| measured`; tests, smoke dry-run, and existing `npm test` pass. |
| 2 | Evidence proves it | ✅ | Before `adc4645` / after `5cb9f23`, same machine, `captures.tsv` from `factory-prove.sh`. Before: four packages, `apps/runtime` absent, `npm test` exit 0. After: `providers` / `evals` / `runtime` present, still no `apps/runtime`, tests 0-fail, smoke prints `privacy=local-only fallbacks: []` and `local-only without ollama selected: null`. “What this does not prove” records that live providers and a real Control were absent. |
| 3 | Structure holds | ✅ | Edge (`packages/runtime/src/smoke.ts`) calls `route` / `execute`. Service packages `@ark/runtime` and `@ark/evals` take typed values, not framework request types. Adapters (`packages/providers`) own `fetch` and SigV4. Dependencies point down; `zod` was authorized by the spec and declared in `EVIDENCE.md`. |
| 4 | Fails safely | ✅ | Provider HTTP has a timeout (`packages/providers/src/http.ts:10`, 30s default). Ingest times out at 2500ms (`packages/runtime/src/telemetry.ts:99`) and never fails the completion (`packages/runtime/src/telemetry.ts:82`). Requests are Zod-parsed at the boundary (`packages/runtime/src/execute.ts:31`). Policy refusal is `PolicyError` with rationale (`packages/runtime/src/execute.ts:53`). Fallback is bounded; `maxTokens` is capped at 8192 on the request schema (`packages/runtime/src/types.ts:35`). Credentials come from env. |
| 5 | Readable | ✅ | Packages match the spec layout. Why-comments sit on the load-bearing decisions (local-only before rank, fallback will not widen the chain, ingest must not throw, host omitted from SigV4 headers). Tests live next to routing, policy, normalization, fallback, telemetry, and eval. No dead or commented-out code. |

**Blocking:** none.
**Non-blocking:** `packages/runtime/src/catalog.ts:11` maps every unmatched Ollama id to `local-8b` with no comment on why a 32B (or similar) model should inherit 8B catalog pricing. `packages/providers/src/http.ts:31` slices provider error bodies into `ProviderError`; that string can reach Control as `errorKind` at `packages/runtime/src/telemetry.ts:63`. `evidence/runtime-v01/after-packages.txt:1` is a synthesized listing rather than the same `ls` shape as `before-packages.txt`; a captured `npm run build` log is also missing (claimed only in `EVIDENCE.md`).
